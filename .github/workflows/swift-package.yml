name: Swift Package

on: [push]

jobs:
  build:
    runs-on: macos-15

    steps:
    - uses: actions/checkout@v4
    - uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.4.0'
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
